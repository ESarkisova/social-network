let initialState = {
    friendList: [
        {id: 11, name: "Anton", ava: null},
        {
            id: 12,
            name: "Grisha",
            ava: null
        },
    ]
};
const sidebarReducer = (state = initialState, action) => {
    return state;
};

export default sidebarReducer;